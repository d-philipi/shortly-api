users: {
    id,
    name,
    email,
    password
}

sessions: {
    id,
    user_id,
    token
}

urls: {
    id ,
    url,
    short_url,
    user_id
}

visits: {
    id,
    short_url,
    visit
}